# Frontend Features

Các feature dự kiến:

`auth`, `master-data`, `purchase-orders`, `goods-receipts`, `warehouse-stock`, `branch-requests`, `daily-count`, `wastage`, `dashboard`, `ai-assistance`.

Mỗi feature nên có page, components, API client và permission states riêng.
